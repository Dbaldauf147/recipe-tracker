import React from 'react';

// Placeholder so the production build resolves. Real saved-lists UI is
// still WIP — this surface keeps the Saved tab navigable without crashing.
export function SavedShoppingLists() {
  return (
    <div style={{ padding: '1.5rem', color: 'var(--color-text-muted)' }}>
      Saved shopping lists — coming soon.
    </div>
  );
}
